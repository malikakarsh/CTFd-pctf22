version: '3'

services:
  ctfd:
    build:
      context: ./
      dockerfile: Dockerfile.prod
    user: root
    restart: always
    ports:
      - "8000:8000"
    environment:
      - UPLOAD_PROVIDER=s3
      - AWS_ACCESS_KEY_ID=GOOG1EB2LF64ZIHPKIOMHSH7SEBQAORMGUG7G5NMNBSEXP44MLTNZMAKXODNI
      - AWS_SECRET_ACCESS_KEY=60OPbOggEHSd9QcH2bXuuT/QHHeVpc7zlfHs85hJ
      - AWS_S3_BUCKET=pctf-bucket
      - AWS_S3_ENDPOINT_URL=https://storage.googleapis.com
      - DATABASE_URL=mysql+pymysql://root:lOFOHWL0oKLKwURHaiMjwywwV874CSz4B1v3amwSNCxmduyrTq@ctf.anirudhvs.xyz:9090/ctfd
      - REDIS_URL=redis://:LOuQal3VPzopuNwnxeUsDtKNoecbEmyPbcunNK8JLChz2IwECt@ctf.anirudhvs.xyz:10000
      - SECRET_KEY=randomstringtobeenteredhere
      - WORKERS=1
      - WORKER_CLASS=gevent
      - TIMEOUT=60
      - LOG_FOLDER=-
      - ACCESS_LOG=-
      - ERROR_LOG=-
      - REVERSE_PROXY=true
    volumes:
      - .data/CTFd/logs:/var/log/CTFd
      - .data/CTFd/uploads:/var/uploads
      - .:/opt/CTFd:ro

#    depends_on:
#      - db
#    networks:
#        default:
#        internal:

#  db:
#    image: mariadb:10.4.12
#    restart: always
#    environment:
#      - MYSQL_ROOT_PASSWORD=ctfd
#      - MYSQL_USER=ctfd
#      - MYSQL_PASSWORD=ctfd
#      - MYSQL_DATABASE=ctfd
#    volumes:
#      - .data/mysql:/var/lib/mysql
#    networks:
#        internal:
#    # This command is required to set important mariadb defaults
#    command: [mysqld, --character-set-server=utf8mb4, --collation-server=utf8mb4_unicode_ci, --wait_timeout=28800, --log-warnings=0]

#  cache:
#    image: redis:4
#    restart: always
#    volumes:
#    - .data/redis:/data
#    networks:
#        internal:

#networks:
#    default:
#    internal:
#        internal: true
